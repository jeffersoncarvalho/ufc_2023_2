import { StyleSheet } from "react-native";

const estilos = StyleSheet.create({
    container: {
        flex: 1,
        alignItems:"center",
        marginTop:30
    },
    header: {
        fontSize:30,
        fontWeight:"bold",
        color:"brown"
    },
    description: {
        fontSize:20,
        color:"black",
        textAlign:"center"
    }
})

export default estilos